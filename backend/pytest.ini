[pytest]
# Add the backend directory to sys.path so "from app.xxx import yyy" works.
pythonpath = .
testpaths = tests
# pytest-asyncio 0.23+ defaults to strict mode; auto is needed for async fixtures.
asyncio_mode = auto
